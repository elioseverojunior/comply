import{_ as t,o,c as a,a2 as s}from"./chunks/framework.BDqW4O7M.js";const m=JSON.parse('{"title":"comply -- Architecture Plan","description":"","frontmatter":{},"headers":[],"relativePath":"plan/IMPLEMENTATION.md","filePath":"plan/IMPLEMENTATION.md","lastUpdated":1785371760000}'),i={name:"plan/IMPLEMENTATION.md"};function n(c,e,r,d,l,p){return o(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="comply-architecture-plan" tabindex="-1">comply -- Architecture Plan <a class="header-anchor" href="#comply-architecture-plan" aria-label="Permalink to &quot;comply -- Architecture Plan&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>comply is a <strong>REUSE compliance tool</strong> written in pure Rust. It is a new implementation (not a port of the Python <a href="https://github.com/fsfe/reuse-tool" target="_blank" rel="noreferrer">fsfe/reuse-tool</a>), but it maintains full configuration compatibility with the REUSE specification (REUSE.toml / DEP5 format).</p><p>The goal is to provide a fast, secure, and multi-platform REUSE compliance toolkit that works everywhere: CLI, browser (WASM), IDE (LSP), and AI-assisted coding (MCP).</p><h2 id="guiding-principles" tabindex="-1">Guiding principles <a class="header-anchor" href="#guiding-principles" aria-label="Permalink to &quot;Guiding principles&quot;">​</a></h2><ul><li><strong>TDD</strong>: Test first, implement, refactor. Never production code before tests.</li><li><strong>Security-first</strong>: No unwrap/expect on external input; <code>thiserror</code> in libraries, <code>anyhow</code> in binaries. Unsafe code forbidden workspace-wide.</li><li><strong>REUSE Specification compliant</strong>: Output and configuration are interchangeable with the Python reuse-tool. Same REUSE.toml format, same lint rules.</li><li><strong>KISS, DRY, YAGNI, TDA, SOLID</strong>.</li></ul><h2 id="crate-architecture" tabindex="-1">Crate architecture <a class="header-anchor" href="#crate-architecture" aria-label="Permalink to &quot;Crate architecture&quot;">​</a></h2><p>The project is a virtual workspace with 5 crates under <code>crates/</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Cargo.toml                     # Virtual workspace root ([workspace] only)</span></span>
<span class="line"><span>crates/</span></span>
<span class="line"><span>  comply/                        # Core library (REUSE compliance engine)</span></span>
<span class="line"><span>    src/</span></span>
<span class="line"><span>      lib.rs</span></span>
<span class="line"><span>      spdx.rs                    # SPDX expression parsing and validation</span></span>
<span class="line"><span>      header.rs                  # SPDX header detection, parsing, generation</span></span>
<span class="line"><span>      license.rs                 # License detection, known license DB</span></span>
<span class="line"><span>      file.rs                    # File discovery, classification, .license files</span></span>
<span class="line"><span>      config.rs                  # REUSE.toml / DEP5 parsing</span></span>
<span class="line"><span>      report.rs                  # Lint reports, BOM generation</span></span>
<span class="line"><span>      hash.rs                    # Integrity hashing (SHA256)</span></span>
<span class="line"><span>      error.rs                   # Error types</span></span>
<span class="line"><span>  comply-cli/                    # CLI binary</span></span>
<span class="line"><span>    src/</span></span>
<span class="line"><span>      main.rs                    # Entrypoint + subcommand dispatch</span></span>
<span class="line"><span>      init.rs                    # \`comply init\` - initialize project</span></span>
<span class="line"><span>      format.rs                  # \`comply format\` - format SPDX headers</span></span>
<span class="line"><span>      lint.rs                    # \`comply lint\` - check compliance</span></span>
<span class="line"><span>      annotate.rs                # \`comply annotate\` - add/update headers</span></span>
<span class="line"><span>      fix.rs                     # \`comply fix\` - auto-fix issues</span></span>
<span class="line"><span>      intelligent.rs             # AI-assisted compliance management</span></span>
<span class="line"><span>  comply-wasm/                   # WASM target (browser)</span></span>
<span class="line"><span>    src/</span></span>
<span class="line"><span>      lib.rs                     # wasm-bindgen API surface</span></span>
<span class="line"><span>      tree.rs                    # File tree analysis</span></span>
<span class="line"><span>      report.rs                  # Report generation</span></span>
<span class="line"><span>  comply-mcp/                    # MCP (Model Context Protocol) server</span></span>
<span class="line"><span>    src/</span></span>
<span class="line"><span>      main.rs                    # MCP server entrypoint</span></span>
<span class="line"><span>      tools.rs                   # Tool definitions</span></span>
<span class="line"><span>      server.rs                  # MCP protocol handling</span></span>
<span class="line"><span>  comply-lsp/                    # Language Server Protocol</span></span>
<span class="line"><span>    src/</span></span>
<span class="line"><span>      main.rs                    # LSP server entrypoint</span></span>
<span class="line"><span>      backend.rs                 # Document analysis backend</span></span>
<span class="line"><span>      handlers.rs                # LSP notification/request handlers</span></span>
<span class="line"><span>      diagnostics.rs             # Diagnostic generation</span></span>
<span class="line"><span>      code_actions.rs            # Code action providers</span></span></code></pre></div><h2 id="crate-dependency-graph" tabindex="-1">Crate dependency graph <a class="header-anchor" href="#crate-dependency-graph" aria-label="Permalink to &quot;Crate dependency graph&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>comply-lsp  comply-mcp  comply-wasm  comply-cli</span></span>
<span class="line"><span>     \\          |          /         /</span></span>
<span class="line"><span>      \\         |         /         /</span></span>
<span class="line"><span>       comply (core library)</span></span></code></pre></div><p><code>comply</code> is the sole library dependency for all other crates. Each surface crate (CLI, WASM, MCP, LSP) depends only on <code>comply</code> + its transport-layer dependencies.</p><h2 id="layer-architecture" tabindex="-1">Layer architecture <a class="header-anchor" href="#layer-architecture" aria-label="Permalink to &quot;Layer architecture&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>+------------------------------------------------------+</span></span>
<span class="line"><span>|  Surface layers                                       |</span></span>
<span class="line"><span>|  +----------+  +----------+  +----------+  +--------+ |</span></span>
<span class="line"><span>|  | comply-  |  | comply-  |  | comply-  |  |comply- | |</span></span>
<span class="line"><span>|  | cli      |  | wasm     |  | mcp      |  | lsp    | |</span></span>
<span class="line"><span>|  +----------+  +----------+  +----------+  +--------+ |</span></span>
<span class="line"><span>+------------------------------------------------------+</span></span>
<span class="line"><span>|  Core library (comply)                                 |</span></span>
<span class="line"><span>|  +--------+ +--------+ +--------+ +--------+ +------+ |</span></span>
<span class="line"><span>|  | spdx   | | header | | license| | file   | |config| |</span></span>
<span class="line"><span>|  +--------+ +--------+ +--------+ +--------+ +------+ |</span></span>
<span class="line"><span>|  +--------+ +--------+                               |</span></span>
<span class="line"><span>|  | report | | hash   |                               |</span></span>
<span class="line"><span>|  +--------+ +--------+                               |</span></span>
<span class="line"><span>+------------------------------------------------------+</span></span></code></pre></div><h2 id="module-design" tabindex="-1">Module design <a class="header-anchor" href="#module-design" aria-label="Permalink to &quot;Module design&quot;">​</a></h2><h3 id="comply-core-library" tabindex="-1"><code>comply</code> (core library) <a class="header-anchor" href="#comply-core-library" aria-label="Permalink to &quot;\`comply\` (core library)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Module</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>spdx</code></td><td>Parse and validate SPDX expressions (<code>MIT</code>, <code>Apache-2.0</code>, <code>MIT OR Apache-2.0</code>, <code>LicenseRef-*</code>). Validate license IDs against the SPDX License List.</td></tr><tr><td><code>header</code></td><td>Detect SPDX headers in files (first N lines regex scan). Parse <code>SPDX-FileCopyrightText</code> and <code>SPDX-License-Identifier</code> lines. Generate correctly formatted headers.</td></tr><tr><td><code>license</code></td><td>License detection from text (approximate matching). Bundled SPDX License List data for known licenses. Custom license support via <code>.license</code> files.</td></tr><tr><td><code>file</code></td><td>File discovery (recursive directory walk). File classification (source, binary, build artifact, ignored). <code>.license</code> companion file handling. Gitignore-aware.</td></tr><tr><td><code>config</code></td><td>Parse <code>REUSE.toml</code> (TOML-based manifest) and <code>.reuse/dep5</code> (DEP5 format). Both formats per REUSE spec.</td></tr><tr><td><code>report</code></td><td>Generate lint reports (compliant vs noncompliant files). Bill of Materials (BOM) generation. Machine-readable output (JSON, SPDX).</td></tr><tr><td><code>hash</code></td><td>SHA256 file hashing for integrity checks.</td></tr><tr><td><code>error</code></td><td>Typed error enum using <code>thiserror</code>.</td></tr></tbody></table><h3 id="comply-cli-cli-binary" tabindex="-1"><code>comply-cli</code> (CLI binary) <a class="header-anchor" href="#comply-cli-cli-binary" aria-label="Permalink to &quot;\`comply-cli\` (CLI binary)&quot;">​</a></h3><p>Subcommands mirror the Python reuse-tool for compatibility:</p><table tabindex="0"><thead><tr><th>Subcommand</th><th>Description</th></tr></thead><tbody><tr><td><code>init</code></td><td>Create <code>.reuse/</code> directory with initial <code>REUSE.toml</code> and <code>LICENSES/</code> structure.</td></tr><tr><td><code>format</code></td><td>Normalize SPDX headers across all files (consistent formatting, ordering, line endings).</td></tr><tr><td><code>lint</code></td><td>Check the entire project for REUSE compliance. Exit 0 if compliant, non-zero with report.</td></tr><tr><td><code>annotate</code></td><td>Add or update SPDX headers on specified files. Supports <code>--copyright</code>, <code>--license</code>, <code>--year</code>, etc.</td></tr><tr><td><code>fix</code></td><td>Auto-fix common compliance issues (missing headers, wrong format, missing licenses).</td></tr><tr><td><code>lint-file</code></td><td>Check only the named files. The pre-commit shape: cost is proportional to the change, not the repository. Defaults to <code>--lines</code>, matching the reference tool.</td></tr><tr><td><code>download</code></td><td>Fetch licence texts into <code>LICENSES/</code>. <code>--all</code> fetches exactly what <code>lint</code> reports missing, which is what makes it the remedy for that finding rather than a separate chore.</td></tr><tr><td><code>spdx</code></td><td>Emit an SPDX 2.1 tag-value bill of materials.</td></tr><tr><td><code>convert-dep5</code></td><td>Convert <code>.reuse/dep5</code> into an equivalent <code>REUSE.toml</code> and remove the dep5, so the project stops declaring licensing two ways.</td></tr><tr><td><code>supported-licenses</code></td><td>List the bundled SPDX identifiers (727 licences, 34 exceptions, 32 deprecated).</td></tr></tbody></table><p>Output flags on <code>lint</code> and <code>lint-file</code> mirror <code>reuse</code> 6.2.0: <code>--quiet</code>, <code>--json</code>, <code>--plain</code>, <code>--lines</code>. Precedence is resolved in one place (<code>output::Format</code>), and the default differs per command -- <code>plain</code> for <code>lint</code>, <code>lines</code> for <code>lint-file</code>.</p><p>Three deliberate divergences from the reference tool, all recorded here so they are not mistaken for drift:</p><ul><li><code>spdx</code> checksums with <code>SHA256</code>, not <code>SHA1</code>. Both are valid SPDX; SHA-1 is no longer fit for integrity claims.</li><li><code>spdx</code> derives <code>DocumentNamespace</code> from the file set rather than a random UUID, so an unchanged tree yields a byte-identical document and two SBOMs can be diffed in review.</li><li><code>convert-dep5</code> refuses to overwrite an existing <code>REUSE.toml</code> instead of replacing it, because that file may hold annotations the conversion knows nothing about.</li></ul><p>The git-tracked backfill has to judge ancestors that a walk would never reach. <code>out_of_scope</code> prunes a directory only when handed the directory itself, which is all a descent needs -- it stops there and never sees the contents. The backfill starts from <code>git ls-files</code>, so its paths skipped that descent entirely: <code>LICENSES/MIT.txt</code> was pruned from the walk, tracked by git, absent from <code>seen</code>, and re-added with <code>is_dir: false</code>, which bypasses the one rule that would have caught it. <code>is_excluded</code> already had the component-wise check and a comment explaining precisely this hazard; the backfill did not use it. <code>scope::is_within_pruned_dir</code> now holds that rule once and both call it.</p><p>The visible damage was in the SBOM, which asserted that <code>LICENSES/MIT.txt</code> is licensed <code>MIT OR Apache-2.0</code> through the <code>**</code> aggregate -- claiming the MIT licence text is covered by the project&#39;s dual licence.</p><p><code>spdx</code> describes only the files <code>lint</code> checks: a <code>Status::Skipped</code> file is left out entirely rather than listed as <code>NOASSERTION</code>. It has no licensing to state, and an entry would assert &quot;unknown&quot; about a file the manifest may well cover -- the <code>**</code> aggregate covers <code>LICENSE</code>, yet the entry said otherwise. This surfaced only when <code>LICENSE</code> stopped being a symlink into <code>LICENSES/</code> and became a regular dual-licence notice, since the symlinks were already excluded. The invariant to hold on to is that the SBOM&#39;s file count equals the number <code>lint</code> reports checked.</p><p><code>download</code> shells out to <code>curl</code> rather than linking an HTTP/TLS stack. That is a supply-chain choice: the project carries zero <code>cargo-vet</code> exemptions and audits every dependency through imported audit sets, and a TLS client would add a large subtree needing fresh audits for a feature used once per licence. <code>--source</code> takes a URL or path template, so the tests never touch the network.</p><p>The <code>intelligent.rs</code> module provides AI-assisted features:</p><ul><li>Smart file classification using ML heuristics</li><li>Automatic license detection from project context</li><li>Batch annotation with conflict detection</li><li>Integration with comply-mcp for agent assistance</li></ul><h3 id="comply-wasm-wasm-binary" tabindex="-1"><code>comply-wasm</code> (WASM binary) <a class="header-anchor" href="#comply-wasm-wasm-binary" aria-label="Permalink to &quot;\`comply-wasm\` (WASM binary)&quot;">​</a></h3><p>Exposes core compliance functions to browser JavaScript:</p><ul><li><code>lint_project(files: &amp;[FileEntry]) -&gt; LintReport</code> -- check compliance of uploaded project files</li><li><code>annotate_file(source: &amp;str, license: &amp;str, copyright: &amp;str) -&gt; String</code> -- add SPDX header to file content</li><li><code>validate_spdx(expression: &amp;str) -&gt; ValidationResult</code> -- validate an SPDX expression</li><li><code>parse_reuse_toml(content: &amp;str) -&gt; Result&lt;Config, Error&gt;</code> -- parse REUSE.toml in-browser</li></ul><p>Target: <code>wasm32-unknown-unknown</code>. No filesystem access (all data supplied from JS). Uses <code>wasm-bindgen</code> for the JS bridge.</p><h3 id="comply-mcp-mcp-server" tabindex="-1"><code>comply-mcp</code> (MCP server) <a class="header-anchor" href="#comply-mcp-mcp-server" aria-label="Permalink to &quot;\`comply-mcp\` (MCP server)&quot;">​</a></h3><p>Implements the <a href="https://modelcontextprotocol.io" target="_blank" rel="noreferrer">Model Context Protocol</a> to expose compliance tools to AI coding assistants (Claude, Copilot, etc.).</p><p>Resources:</p><ul><li><code>reuse://project/{path}/lint</code> -- lint results for a project</li><li><code>reuse://project/{path}/files</code> -- file classification tree</li><li><code>reuse://project/{path}/bom</code> -- bill of materials</li></ul><p>Tools:</p><ul><li><code>lint_project(path: str)</code> -- run REUSE lint on a project</li><li><code>annotate_file(path: str, license: str, copyright: str, year: str)</code> -- annotate a single file</li><li><code>init_project(path: str)</code> -- initialize REUSE structure</li><li><code>check_spdx(expression: str)</code> -- validate an SPDX expression</li><li><code>list_licenses()</code> -- list known SPDX licenses</li></ul><p>Prompts:</p><ul><li><code>compliance_status</code> -- summary of a project&#39;s REUSE compliance</li><li><code>fix_plan</code> -- step-by-step fix plan for compliance issues</li></ul><h3 id="comply-lsp-lsp-server" tabindex="-1"><code>comply-lsp</code> (LSP server) <a class="header-anchor" href="#comply-lsp-lsp-server" aria-label="Permalink to &quot;\`comply-lsp\` (LSP server)&quot;">​</a></h3><p>Implements the <a href="https://microsoft.github.io/language-server-protocol/" target="_blank" rel="noreferrer">Language Server Protocol</a> to provide real-time compliance feedback in editors (VS Code, Neovim, Emacs, etc.).</p><p>Capabilities:</p><ul><li><strong>Diagnostics</strong>: flag files missing SPDX headers, invalid SPDX expressions, missing license files, incorrect DEP5 coverage</li><li><strong>Code actions</strong>: <ul><li><code>Add SPDX header</code> (inserts a compliant header)</li><li><code>Fix SPDX expression</code> (corrects syntax)</li><li><code>Annotate with license</code> (adds license identifier)</li><li><code>Create .license file</code> (for binary/uncommentable files)</li></ul></li><li><strong>Completion</strong>: suggest SPDX license IDs when typing the <code>SPDX-License-Identifier</code> tag</li><li><strong>Hover</strong>: show license details when hovering over an SPDX expression</li><li><strong>Code lens</strong>: show compliance status at the top of each file</li></ul><p>Transport: stdio (default), TCP (optional).</p><p>Protocol version: 3.17 (LSP 3.17).</p><h2 id="reuse-specification-compatibility" tabindex="-1">REUSE Specification compatibility <a class="header-anchor" href="#reuse-specification-compatibility" aria-label="Permalink to &quot;REUSE Specification compatibility&quot;">​</a></h2><p>The REUSE specification (version 3.x) defines three methods for declaring licenses:</p><ol><li><strong>SPDX headers</strong> -- inline <code>SPDX-License-Identifier</code> and <code>SPDX-FileCopyrightText</code> in source files</li><li><strong><code>.license</code> files</strong> -- companion files for binary/uncommentable files</li><li><strong>DEP5 / REUSE.toml</strong> -- bulk declarations in <code>.reuse/dep5</code> or <code>REUSE.toml</code></li></ol><p>comply implements all three methods identically to the Python reuse-tool, so the two agree on the same project. That parity is a design constraint, not a runtime dependency: the reference tool is not installed, invoked or checked by any task, workflow or hook. Divergences are settled by writing a test here, not by shelling out to it.</p><p>Key compatibility points:</p><ul><li><code>REUSE.toml</code> structure is identical (TOML format, same keys)</li><li><code>.reuse/dep5</code> format is identical (DEP5 format, same keys)</li><li>SPDX header regex patterns match the Python tool&#39;s behavior</li><li><code>.license</code> file naming and content conventions match exactly</li><li>Exit codes match: 0 = compliant, 1 = non-compliant</li></ul><h2 id="implementation-phases" tabindex="-1">Implementation phases <a class="header-anchor" href="#implementation-phases" aria-label="Permalink to &quot;Implementation phases&quot;">​</a></h2><h3 id="phase-1-core-library-done" tabindex="-1">Phase 1 -- Core library (DONE) <a class="header-anchor" href="#phase-1-core-library-done" aria-label="Permalink to &quot;Phase 1 -- Core library (DONE)&quot;">​</a></h3><ul><li><code>comply::spdx</code> -- SPDX expression parser and validator</li><li><code>comply::header</code> -- header detector and generator</li><li><code>comply::file</code> -- file discovery and classification</li><li><code>comply::error</code> -- error types</li><li>Known license DB (SPDX License List 3.28 bundled)</li></ul><h3 id="phase-2-cli-done" tabindex="-1">Phase 2 -- CLI (DONE) <a class="header-anchor" href="#phase-2-cli-done" aria-label="Permalink to &quot;Phase 2 -- CLI (DONE)&quot;">​</a></h3><ul><li><code>comply-cli lint</code> -- full lint implementation</li><li><code>comply-cli annotate</code> -- annotation subcommand</li><li><code>comply-cli init</code> -- project initialization</li><li><code>REUSE.toml</code> / DEP5 parsing support</li><li><code>.license</code> file support</li></ul><h3 id="phase-3-format-and-fix-done" tabindex="-1">Phase 3 -- Format and Fix (DONE) <a class="header-anchor" href="#phase-3-format-and-fix-done" aria-label="Permalink to &quot;Phase 3 -- Format and Fix (DONE)&quot;">​</a></h3><ul><li><code>comply-cli format</code> -- header formatting</li><li><code>comply-cli fix</code> -- auto-fix implementation</li><li><code>comply::report</code> -- report generation (text, JSON, SPDX BOM)</li><li><code>comply::hash</code> -- integrity hashing</li><li><code>comply::license</code> -- license detection from text</li></ul><h3 id="phase-3b-command-surface-parity-with-reuse-6-2-0-done" tabindex="-1">Phase 3b -- Command-surface parity with reuse 6.2.0 (DONE) <a class="header-anchor" href="#phase-3b-command-surface-parity-with-reuse-6-2-0-done" aria-label="Permalink to &quot;Phase 3b -- Command-surface parity with reuse 6.2.0 (DONE)&quot;">​</a></h3><ul><li><code>comply-cli lint-file</code> -- lint named files only</li><li><code>comply-cli download</code> -- fetch licence texts, <code>--all</code> driven by the lint audit</li><li><code>comply-cli spdx</code> -- SPDX 2.1 tag-value SBOM</li><li><code>comply-cli convert-dep5</code> -- DEP5 to REUSE.toml migration</li><li><code>comply-cli supported-licenses</code> -- expose the bundled SPDX lists</li><li><code>comply-cli output</code> -- <code>--quiet/--json/--plain/--lines</code> for both lint commands</li><li><code>comply::config::Dep5Document</code> -- DEP5 header fields and folded continuation lines, which the previous parser dropped</li><li><code>comply::reuse_toml::render_dep5_as_toml</code> -- writer verified byte-identical to <code>reuse convert-dep5</code></li><li><code>comply::spdx::{known_licenses, known_exceptions, deprecated_licenses}</code> -- enumeration of the bundled lists</li><li><code>comply::file::classify_path</code> -- classify one path as a walk would</li></ul><h3 id="phase-4-wasm-pending" tabindex="-1">Phase 4 -- WASM (pending) <a class="header-anchor" href="#phase-4-wasm-pending" aria-label="Permalink to &quot;Phase 4 -- WASM (pending)&quot;">​</a></h3><ul><li><code>comply-wasm</code> -- WASM compilation target</li><li>JS API design</li><li>Browser demo / playground</li></ul><h3 id="phase-5-lsp-pending" tabindex="-1">Phase 5 -- LSP (pending) <a class="header-anchor" href="#phase-5-lsp-pending" aria-label="Permalink to &quot;Phase 5 -- LSP (pending)&quot;">​</a></h3><ul><li><code>comply-lsp</code> -- LSP server</li><li>Diagnostics for missing/invalid headers</li><li>Code actions for compliance fixes</li><li>VS Code extension (separate repo or bundled)</li></ul><h3 id="phase-6-mcp-pending" tabindex="-1">Phase 6 -- MCP (pending) <a class="header-anchor" href="#phase-6-mcp-pending" aria-label="Permalink to &quot;Phase 6 -- MCP (pending)&quot;">​</a></h3><ul><li><code>comply-mcp</code> -- MCP server</li><li>Tool definitions for AI assistants</li><li>Resource providers for project state</li></ul><h2 id="feature-flags" tabindex="-1">Feature flags <a class="header-anchor" href="#feature-flags" aria-label="Permalink to &quot;Feature flags&quot;">​</a></h2><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># comply (core)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">features</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;license-db-bundled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">license-db-bundled = []   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Bundle SPDX License List (~500KB)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">license-db-network = []   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Fetch license list at runtime</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># comply-cli</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">features</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default = []</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">intelligent = []          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># AI-assisted features (requires comply-mcp)</span></span></code></pre></div><h2 id="testing-strategy" tabindex="-1">Testing strategy <a class="header-anchor" href="#testing-strategy" aria-label="Permalink to &quot;Testing strategy&quot;">​</a></h2><ul><li><strong>Unit tests</strong>: Inline <code>#[cfg(test)] mod tests</code> in every module.</li><li><strong>Integration tests</strong>: End-to-end <code>comply lint</code> on fixture projects.</li><li><strong>Fixtures</strong>: Small test projects with known compliance states (compliant, missing headers, invalid SPDX, etc.).</li><li><strong>Round-trip tests</strong>: Parse REUSE.toml -&gt; generate -&gt; re-parse -&gt; compare.</li><li><strong>Compatibility tests</strong>: parity with the reference tool is pinned by tests in this repository -- expectations captured from it and asserted against comply, such as the byte-for-byte header cases in <code>header/generate.rs</code>. The reference tool is not run as part of any gate.</li></ul><h3 id="coverage-one-gate-one-exclusion-list" tabindex="-1">Coverage: one gate, one exclusion list <a class="header-anchor" href="#coverage-one-gate-one-exclusion-list" aria-label="Permalink to &quot;Coverage: one gate, one exclusion list&quot;">​</a></h3><p>Two engines run, but only one decides.</p><ul><li><strong>tarpaulin is the gate of record.</strong> <code>ci.yml</code>&#39;s Coverage job passes or fails the build; <code>tarpaulin.toml</code> holds the threshold rationale and <code>exclude-files</code>.</li><li><strong>llvm-cov runs against a ratchet.</strong> <code>mise.yml</code> holds it to a threshold it can actually meet (currently 97; today&#39;s floor is regions 98.93, functions 98.00, lines 99.74), so that job goes red on a real regression rather than on every run. It cannot be held to 100: the shortfall is <code>?</code> error edges and assert branches that no test can execute. Raise the ratchet as the numbers improve.</li><li><strong>Exclusions are declared once.</strong> The <code>llvm-cov</code> task derives its <code>--ignore-filename-regex</code> from <code>tarpaulin.toml</code>&#39;s <code>exclude-files</code> at run time. It used to keep a hand-copied regex, so the two could drift and the verdict would depend on which engine ran.</li></ul><p>Two lines needed source changes rather than tests to become measurable, both because tarpaulin attributes a line it cannot observe being executed:</p><ul><li>a <code>const fn</code> whose test passed literal arguments was folded at compile time -- the test now hides them behind <code>std::hint::black_box</code></li><li>a branch keyword on its own line inside a <code>filter_map</code> closure -- the code is now a single <code>then_some</code> expression with no <code>if</code>/<code>else</code> statement</li></ul><h2 id="key-design-decisions" tabindex="-1">Key design decisions <a class="header-anchor" href="#key-design-decisions" aria-label="Permalink to &quot;Key design decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>REUSE spec compatible</td><td>Drop-in replacement for Python reuse-tool; same config, same output</td></tr><tr><td>Native Rust, not a port</td><td>Full safety guarantees, no Python dependency, cross-compilation to WASM</td></tr><tr><td>Multi-surface architecture</td><td>One core library powers CLI, WASM, LSP, and MCP</td></tr><tr><td>Unsafe code forbidden</td><td><code>unsafe_code = &quot;deny&quot;</code> workspace-wide</td></tr><tr><td>Stable Rust</td><td><code>rust-toolchain.toml</code> pins stable, edition 2024</td></tr><tr><td>Bundled SPDX License List</td><td>Offline-first: works without network</td></tr><tr><td>TDD strictly</td><td>Every feature starts with a failing test</td></tr></tbody></table><h3 id="an-unknown-extension-is-refused-not-guessed" tabindex="-1">An unknown extension is refused, not guessed <a class="header-anchor" href="#an-unknown-extension-is-refused-not-guessed" aria-label="Permalink to &quot;An unknown extension is refused, not guessed&quot;">​</a></h3><p><code>detect_style</code> used to end in <code>CommentStyle::Hash</code> for anything its tables did not claim. That made &quot;no built-in knows this format&quot; indistinguishable from &quot;this format uses <code>#</code>&quot;, and the wrong one was written to disk: <code>.mts</code> got shell comments in a TypeScript file, as did <code>.tsx</code>, <code>.jsx</code>, <code>.mjs</code>, <code>.cjs</code>, <code>.vue</code>, <code>.lua</code>, and <code>.bat</code>. <code>--force</code> then made it stick.</p><p>Unclaimed names now resolve to <code>CommentStyle::Uncommentable</code>. Nothing new was needed downstream: <code>commands/annotate.rs</code> and <code>commands/format.rs</code> already branch on that variant to warn, honour <code>--skip-unrecognised</code>, or write a <code>.license</code> companion, which is the contract reuse 6.2.0 offers for the same case. The variant&#39;s meaning widens from &quot;binary&quot; to &quot;no known comment syntax&quot;, and the messages say so.</p><p>Three consequences worth stating:</p><ul><li><strong>Lint is untouched.</strong> <code>commands/lint.rs</code> and <code>report.rs</code> never call <code>detect_style</code>; header parsing is permissive by design so an existing header is read whatever comply would have written. The compliance gate is unchanged.</li><li><strong>A name table was required.</strong> With the <code>#</code> catch-all gone, <code>Dockerfile</code>, <code>Makefile</code>, <code>CODEOWNERS</code>, and dotfiles like <code>.gitignore</code> had nothing to fall back on, so <code>filename_style</code> now maps exact names. It is consulted before the extension tables, because <code>CMakeLists.txt</code> is a build script rather than the unrecognised <code>.txt</code> its suffix suggests.</li><li><strong>The extension was being read from the wrong string.</strong> <code>detect_style</code> split the whole path on <code>.</code>, so <code>.github/CODEOWNERS</code> resolved through <code>&quot;github/codeowners&quot;</code>. It now takes the basename, as <code>detect_style_with</code> always did.</li></ul><p>The escape hatch is <code>[[tool.comply.comment-styles]]</code>, which <code>detect_style_with</code> already consults ahead of the built-ins. <code>Uncommentable</code> is therefore a prompt to declare the syntax, not a dead end.</p><h3 id="tool-comply-ignore-carries-two-intents" tabindex="-1"><code>[tool.comply] ignore</code> carries two intents <a class="header-anchor" href="#tool-comply-ignore-carries-two-intents" aria-label="Permalink to &quot;\`[tool.comply] ignore\` carries two intents&quot;">​</a></h3><p>Most entries mean <em>not part of the project</em> (<code>**/target/**</code>, <code>**/*.lock</code>). A few mean <em>part of the project, but nothing may write to it</em>: <code>DCO.txt</code>, whose own text forbids modification, and the <code>crates/*/LICENSE-*</code> symlinks, which <code>annotate</code> would write straight through into the canonical licence texts.</p><p>The walk cannot tell the two apart, so it dropped the second kind entirely -- including its declaration. <code>DCO.txt</code> is the only carrier of <code>CC-BY-3.0</code>, so the licence it declares at <code>REUSE.toml:14</code> never reached <code>used_licenses</code> and <code>license::audit</code> called the text unused. The reference tool, which has no such config, correctly reported it used; comply failed CI over a licence plainly in use.</p><p><code>collect_declared_by_ignored</code> closes the gap: an annotation whose file exists but is excluded contributes its identifiers to the used set, without the file being judged or counted. The existence check is what keeps this honest -- a stale annotation naming a deleted path would otherwise keep a dead licence text alive forever, which is exactly what the unused check exists to catch. It also confines the rule to literal paths, since a glob such as <code>crates/*/LICENSE-*</code> names no file on disk.</p><p>The fuller fix is to split the config so write-protection stops overloading scope. That is a user-facing schema change and is deliberately not done here; this narrows the damage without moving the schema.</p><h2 id="reuse-configuration-compatibility" tabindex="-1">REUSE configuration compatibility <a class="header-anchor" href="#reuse-configuration-compatibility" aria-label="Permalink to &quot;REUSE configuration compatibility&quot;">​</a></h2><p>comply reads and writes:</p><ul><li><strong><code>REUSE.toml</code></strong> (preferred, TOML format) -- same structure as Python reuse-tool</li><li><strong><code>.reuse/dep5</code></strong> (DEP5 format) -- same structure as Python reuse-tool</li><li><strong>Inline SPDX headers</strong> -- same regex patterns</li></ul><p>comply generates identical output to the Python tool for:</p><ul><li><code>lint</code> output (text format, same messages)</li><li><code>lint --lines</code> and <code>lint-file</code> (verified byte-identical on shared fixtures)</li><li><code>convert-dep5</code> output (verified byte-identical)</li><li>Header annotation format (all nine shared comment styles)</li></ul><p><code>spdx</code> is tag-value rather than JSON, and diverges deliberately on checksum algorithm and namespace derivation -- see the CLI section above.</p><p>This ensures projects can switch between <code>reuse</code> and <code>comply</code> seamlessly.</p><h2 id="backlog-needs-analysis-before-implementation" tabindex="-1">Backlog -- needs analysis before implementation <a class="header-anchor" href="#backlog-needs-analysis-before-implementation" aria-label="Permalink to &quot;Backlog -- needs analysis before implementation&quot;">​</a></h2><h3 id="user-defined-comment-markers-in-reuse-toml" tabindex="-1">User-defined comment markers in REUSE.toml <a class="header-anchor" href="#user-defined-comment-markers-in-reuse-toml" aria-label="Permalink to &quot;User-defined comment markers in REUSE.toml&quot;">​</a></h3><p>Comment syntax is currently a closed set: <code>CommentStyle</code> has nine variants and <code>detect_style</code> maps extensions onto them in <code>crates/comply/src/header/style.rs</code>. A project using a format comply does not know is refused rather than annotated (see &quot;An unknown extension is refused, not guessed&quot;) -- there is no way to teach it a new syntax without editing the crate.</p><p>The proposal is to let a project declare markers itself. Settled shape, an array of tables so one rule can cover every language that shares a syntax:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">comply</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">comment-styles</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">extensions = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;vue&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;svelte&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;astro&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">open  = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;!--&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # emitted once, on its own line; omit for line styles</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">line  = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # prefix on each header line; omit when there is none</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">close = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;--&gt;&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # emitted once; omit for line styles</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">comply</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">comment-styles</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">filenames = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Jenkinsfile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Vagrantfile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">open  = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;#&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # a line style sets \`open\` only</span></span></code></pre></div><p><code>extensions</code> and <code>filenames</code> are both optional and may appear together; a rule with neither is a configuration error rather than a silent no-op. Grouping is the point -- eleven extensions share <code>#</code> in the built-in table today, and a table per key would mean eleven near-identical blocks.</p><p>Two spelling notes. An inline <code>{ ... }</code> table cannot span lines in TOML, so the obvious-looking nested-map form does not parse. And the doubled brackets matter: <code>[[...]]</code> is an array of tables, <code>[...]</code> a single one, so a second <code>[...]</code> block of the same name is a duplicate-key error.</p><p>Grouping costs one thing the keyed form gave free: with a unique key per rule, two rules could not claim the same extension. Here they can, so the loader needs a stated tiebreak, and there is no precedent in the file to borrow -- <code>ignore</code> resolves by <code>.any()</code>, a union in which order carries no meaning, so nothing about it implies an answer here. The tiebreak has to be chosen and written down. Last rule wins is the conventional pick; rejecting the manifest outright is the safer one, since a duplicated extension is far more likely a mistake than an intent.</p><p><strong>Settled: reject.</strong> <code>reject_contested_keys</code> in <code>config/reuse_toml.rs</code> refuses a manifest where two rules claim the same extension or filename, naming the key. Rejecting means the order of a <code>Vec</code> never decides anything, so <code>detect_style_with</code>&#39;s first-match lookup needs no documented precedence -- at most one rule can ever match. Extensions compare lowercased, matching how lookup normalises them, so <code>VUE</code> and <code>vue</code> collide rather than shadow. Filenames compare exactly, since a filesystem may distinguish them. The two are separate namespaces: a <code>makefile</code> extension does not contest a <code>makefile</code> filename.</p><h3 id="how-a-configured-style-reaches-the-code-that-writes-it" tabindex="-1">How a configured style reaches the code that writes it <a class="header-anchor" href="#how-a-configured-style-reaches-the-code-that-writes-it" aria-label="Permalink to &quot;How a configured style reaches the code that writes it&quot;">​</a></h3><p><code>CommentStyle::Custom(u16)</code> is only an index; the delimiters live in the manifest. Carrying them in the enum would drop <code>Copy</code> and ripple through the ~114 places a style is passed, so the index stays and something else has to supply the table.</p><p><code>ResolvedStyle&lt;&#39;a&gt;</code> is that something: a <code>Copy</code> pair of the style and the rules it may index into, built by <code>ResolvedStyle::detect(path, rules)</code> at the four entry points that know both (<code>fix.rs</code> twice, <code>commands/format.rs</code>, <code>commands/annotate.rs</code>). It <strong>replaces</strong> the <code>CommentStyle</code> parameter rather than joining it, which matters because <code>annotate_file_with</code> already sits at clippy&#39;s seven-argument ceiling -- an eighth parameter would not build under <code>-D warnings</code>. It also retires the runtime refusal <code>generate_with</code> used to carry: an unresolved style no longer type-checks, so the failure moved from a returned <code>Error</code> to a compile error.</p><p>The reach turned out to be wider than rendering. A configured style has to be recognised on the way back in, and four separate places had each hardcoded the built-in list:</p><table tabindex="0"><thead><tr><th>Site</th><th>What it decides</th><th>Was</th></tr></thead><tbody><tr><td><code>generate_with</code></td><td>how the header is written</td><td><code>comment_delimiters</code></td></tr><tr><td><code>comment_mask</code></td><td>which lines are inside a comment</td><td><code>[&quot;&lt;!--&quot;, &quot;/*&quot;, &quot;(*&quot;]</code></td></tr><tr><td><code>carries_licensing</code></td><td>whether a line&#39;s tag is readable</td><td><code>strip_comment_prefix</code></td></tr><tr><td><code>is_block_closer</code></td><td>where the header span ends</td><td><code>&quot;*/&quot; | &quot;*)&quot; | &quot;--&gt;&quot;</code></td></tr></tbody></table><p>All four now derive from the same rules, via <code>block_delimiters(rules)</code> and <code>configured_prefixes(rules)</code>. Missing any one of them is not a cosmetic failure: with the closer alone left hardcoded, <code>header_span</code> ended above a configured closer, so each <code>comply fix</code> left another orphaned <code>--%&gt;</code> behind and the file grew without bound. Two round-trip tests pin it -- annotate-twice and format-after-annotate must both be no-ops, for a line style and a block style.</p><p>Open questions, all of which need answering before this is worth building:</p><ol><li><strong>Is it in the spec?</strong> REUSE 3.3 defines no such key. Adding one makes the file non-portable: <code>reuse</code> would ignore it and annotate the same files differently, so the two tools would fight over the same headers. A <code>[tool.comply]</code> subtable (already used for <code>ignore</code>) keeps it clearly a comply extension rather than a claimed spec feature.</li><li><strong>Generation and detection are two tables.</strong> <code>generate_with</code> renders headers; <code>strip_comment_prefix</code> and <code>detect_comment_style_from_spdx_line</code> read them back. Both would have to be driven from the same config, or comply will write headers it cannot read -- the exact defect that made <code>(*</code> headers undetectable until it was fixed in this cycle.</li><li><strong>The blank separator line.</strong> <code>reuse</code> emits a separator between the copyright block and the licence line for <em>every</em> style (<code>//</code>, <code>#</code>, <code>.. </code>, <code>*</code>); comply emits one only when contributors are present. This is a pre-existing, global divergence: <code>comply format --check</code> reports &quot;would reformat&quot; on any header <code>reuse</code> wrote, for all styles, not just the new ones. Configurable markers would have to decide whether the separator is part of the style, and whether comply&#39;s canonical form should change to match -- which would churn every annotated file in every comply-using project.</li><li><strong>Precedence.</strong> Does a user-defined style override a built-in mapping for the same extension, or only fill gaps?</li></ol><p>Decisions taken (2026-07-27), narrowing the above:</p><ul><li><p><strong>Q1, placement.</strong> <code>[tool.comply.comment-styles]</code>, not a top-level table. Top-level keys in REUSE.toml belong to the spec, and comply <em>silently ignores</em> unknown ones -- a top-level <code>[CommentStyles]</code> would parse and then do nothing, which is the worst outcome of the three.</p></li><li><p><strong>Q2, one source for both directions.</strong> Confirmed as the hard requirement. There is now a third consumer: <code>header_span</code> in <code>header/annotate.rs</code> decides whether a line sits inside a comment, and it currently hardcodes <code>[&quot;&lt;!--&quot;, &quot;/*&quot;, &quot;(*&quot;]</code> -- a second copy of what <code>comment_delimiters</code> already knows. That list is the first thing this feature should delete.</p></li><li><p><strong>Q2a, where a user-defined style lives.</strong> The blocker found while attempting the refactor, and the reason it was reverted rather than finished. Lookup is two hops, <code>extension -&gt; CommentStyle -&gt; (open, close)</code>, and <strong>both ends are closed</strong>: <code>CommentStyle</code> is a fixed ten-variant enum, and <code>comment_delimiters</code> returns <code>(&amp;&#39;static str, &amp;&#39;static str)</code>. A configured style has no variant to map to, and its markers are read from a file at runtime so they cannot be <code>&amp;&#39;static</code>. Merging is therefore not &quot;repoint an extension at a different variant&quot; -- there is no variant to point at.</p><p>Decided: add a <strong>custom variant</strong> carrying owned delimiters, rather than dissolving the enum into config-threaded data. Smaller blast radius, and the enum stays a useful name for the built-ins. The cost is that every exhaustive <code>match</code> on <code>CommentStyle</code> must handle it; <code>comment_delimiters</code> cannot stay <code>const fn</code> returning <code>&amp;&#39;static str</code>, so callers move to an owned or borrowed pair.</p><p>A first attempt at the refactor added <code>ALL_STYLES</code> plus a deliberately exhaustive <code>assert_style_is_listed</code> guard, so that adding a variant breaks the build and points the author at the list. That guard is worth reinstating when the work resumes -- an array of variants is otherwise unchecked.</p></li><li><p><strong>Q4, precedence.</strong> A user rule <em>replaces</em> the built-in for that exact key rather than layering under it, so overriding one extension cannot silently inherit half of the old definition. Built-ins remain the base set and are still what <code>init</code> and <code>convert-dep5</code> write into a generated REUSE.toml.</p></li></ul><p>Q3 (the blank separator line) is unchanged and remains the blocker: it is a global divergence from <code>reuse</code>, not specific to configurable markers.</p><p>Implementation note, learned the hard way this cycle: <code>header_span</code> must decide comment-ness from the file&#39;s <strong>own</strong> style, via <code>detect_style_from_header</code>, not from the style being written. <code>replace_header_with</code> can receive an override (<code>--style</code>, or <code>format --check</code> forcing one), and using that to locate the existing block means <code>--style slash</code> on a Markdown file stops recognising its <code>&lt;!--</code> header. Pin that case with a test before touching the scan.</p><p>Until then the closed set stands, extended in-crate: <code>SlashStar</code> (<code>/* */</code>, for <code>css</code>/<code>scss</code>/<code>sass</code>/<code>less</code>) and <code>DotDot</code> (<code>..</code>, for <code>rst</code>) were added this cycle, leaving <code>json</code> as the only text format routed to a <code>.license</code> companion.</p><h3 id="force-include-list-tool-comply-add" tabindex="-1">Force-include list -- <code>[tool.comply] add</code> <a class="header-anchor" href="#force-include-list-tool-comply-add" aria-label="Permalink to &quot;Force-include list -- \`[tool.comply] add\`&quot;">​</a></h3><p><code>ignore</code> can only subtract. A project that excludes a directory wholesale has no way to bring one file back, short of rewriting the pattern:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">comply</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ignore = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.claude/**/*.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">add    = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.claude/skills/verify/SKILL.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># this one back in</span></span></code></pre></div><p>The design question is not the syntax, it is <strong>where the override stops</strong>.</p><p><code>add</code> overrides the <em>preference</em> layers only -- <code>ignore</code>, gitignore, and <code>[tool.comply.walk]</code>. It must never override the <em>safety</em> layer in <code>scope.rs</code>: <code>.git/</code>, <code>LICENSES/</code>, <code>.reuse/</code>, symlinks, and <code>.license</code> companions.</p><p>That split is not fastidiousness. Both of this cycle&#39;s file-corruption incidents were writes the safety layer is there to stop, and neither was prevented by <code>ignore</code>:</p><ul><li><code>annotate --recursive LICENSES</code> walked with a pruned directory <em>as the root</em>, and the prune matches directory names during descent, so nothing above the root was examined. It wrote an SPDX header into the canonical Apache-2.0 text.</li><li>Before that, an unfiltered explicit path wrote a header into <code>.git/config</code>.</li></ul><p>An <code>add</code> that overrode everything would re-open both, and <code>add = [&quot;**&quot;]</code> is the first thing a frustrated user reaches for. A caller who genuinely needs to write inside <code>LICENSES/</code> can still name the path explicitly to <code>annotate</code>, which is a deliberate act rather than a glob that quietly matches more than intended.</p><p>Open question: whether <code>add</code> re-includes a path for <em>reading</em> (lint judges it) or also for <em>writing</em> (annotate/fix may rewrite it). Reading is clearly safe; writing is the half that needs the safety floor above.</p>`,128)])])}const u=t(i,[["render",n]]);export{m as __pageData,u as default};
